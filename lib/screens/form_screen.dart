import 'package:flutter/material.dart';

class SreenWidget extends StatelessWidget {
  const SreenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Welcome to a new Screen'),
      ),
    );
  }
}
